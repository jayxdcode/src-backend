# env version 1 (deprecated)

PORT=3000
GOOGLE_API_KEY=your_google_key_here
GOOGLE_API_KEY_2=your_second_google_key_or_same_key
GOOGLE_API_KEY_3=''
HTTP_REFERER=https://your-app-domain.com
X_TITLE=SpotifyLyricsBackend


# env version 2 (current)

# Example env for render-server
# Comma-separated API keys (will be used in rotation)
GOOGLE_AI_KEYS=key1,key2,key3

# Optional: custom provider definitions (JSON) if you want to override in runtime
# PROVIDER_DEFINITIONS_JSON=[{"id":"google1","name":"google1_gemini2.0-flash","baseUrl":"https://api.google.example"}]

# Toggle debug logging
DEBUG_RENDER_SERVER=true

# Per-request retry config (tunable)
MAX_RETRIES=3
INITIAL_BACKOFF_MS=200
BACKOFF_MULTIPLIER=2

# Port
PORT=3000